// Return 4 IncompatibleConversion - Semantic Error at Line 12: Invalid assignment between array types

int main() {
    int a = 5;
    int arr[3];
    
    while (a > 0) {  
        a = a - 1;
        if (a == 3) {
            return 0;  
        }
        arr[0] = arr;  
    }
    
    return 0;
}